# G-5.4：管理员侧 LLM 追踪页 iframe 指向。
# 复制为 .env.local 并改成本环境实际地址；前缀 VITE_ 才会被 Vite 注入到浏览器。
VITE_LANGFUSE_URL=http://localhost:3001
# AI 页面总开关。未配置 LLM/RAG 供应商时保持 false，配置完成后改为 true 并重新构建前端。
VITE_AI_ENABLED=false
